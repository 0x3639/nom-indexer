# NoM Indexer Environment Variables
# Copy this file to .env and fill in your values

# Zenon Node WebSocket URL
# The WebSocket endpoint of your Zenon node
# 
# For EXTERNAL node (default):
NODE_URL_WS=ws://127.0.0.1:35998
#
# For LOCAL node (after go-zenon is fully synced):
# Change to: NODE_URL_WS=ws://go-zenon:35998
# This uses the Docker container name for internal networking

# PostgreSQL Database Password
# Set a secure password for the PostgreSQL database
POSTGRES_PASSWORD=changeMeToSecurePassword